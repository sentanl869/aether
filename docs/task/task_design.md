# Aether 设计文档任务跟踪（task_design）

## 1. 目标

- 用于跟踪 `docs/design.md` 各章节编写进度与完成标准。
- 以 `docs/requirements.md` 作为唯一需求基线，确保需求可追踪、可验收、可指导编码实现。

## 2. 全局约束（强制）

- C-001（画图约束）：所有需要画图的任务，必须使用 `mermaid` 语法；禁止使用图片、PlantUML 或其他图形语法。
- C-002（可编码约束）：所有任务完成后，`docs/design.md` 必须达到“可直接指导编码”的粒度，至少覆盖：数据结构、状态流转、接口契约、异常码、并发/幂等、关键流程时序、边界条件。
- C-003（API 约束）：所有 API 接口设计必须遵循 OpenAPI 3.0 规范与 RESTful 风格（资源化路径、标准 HTTP Method、统一响应结构、错误码一致性、幂等与并发控制）。

## 3. 任务拆分与依赖

| Task ID | 任务名称 | 章节范围（design.md） | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| T1 | 基线与追踪框架 | 2、14.4、15.2 | 无 | DONE |
| T2 | 架构与核心模型 | 3、5、6 | T1 | DONE |
| T3 | 契约与执行编排 | 7、8 | T2 | DONE |
| T4 | 功能域与 K8s 编排 | 4、9 | T2、T3 | DONE |
| T5 | 安全、隔离、可运维与 NFR | 10、11、12、13 | T3、T4 | DONE |
| T6 | 验收与交付收口 | 14（除 14.4）、15（除 15.2） | T1~T5 | DONE |
| T7 | 契约一致性与覆盖补充 | 3.5、7、8、11.3、14.4 | T3、T4、T6 | DONE |
| T8 | 契约冲突修订（命名与幂等） | 4.3、5.2.3、7、8、9.4、14.4、15.2 | T7 | DONE |
| T9 | 语义覆盖补完与基线对齐 | 2.6、4.2、4.4、4.8 等（见 T9 明细） | T8 | TODO |

说明：

- 推荐按 `T1 -> T2 -> T3 -> T4 -> T5 -> T6 -> T7 -> T8 -> T9` 顺序推进。
- 若并行执行，必须先冻结跨任务共享基线：状态枚举、错误码、资源命名、审计字段、任务模型。
- 若执行 `T9`，需先复核 `14.4` 当前“已覆盖”条目是否与正文语义一致；存在冲突时先回标 `待补充` 再补正文。

## 4. 任务明细（DoD 驱动）

### T1 基线与追踪框架

- 目标：建立需求到设计的追踪骨架，先消除“写完后无法审计覆盖率”的风险。
- 对应章节：`2`、`14.4`、`15.2`
- 输出物：
- `2` 章完成范围、边界、原则与约束落盘。
- `14.4` 提供逐条需求 ID 覆盖核对表（R-ENV~R-OPS、NFR）。
- `15.2` 定义需求变更进入设计的流程与版本规则。
- DoD：
- 每个需求簇至少映射到一个设计章节。
- 明确“已覆盖/待补充/不适用”三类状态。
- 与 `docs/requirements.md` 无冲突陈述。
- 进度：
- [x] 完成

### T2 架构与核心模型

- 目标：定义统一抽象、领域模型、状态机，作为后续 API 与执行链路的基础。
- 对应章节：`3`、`5`、`6`
- 输出物：
- 系统上下文图、逻辑架构图、核心时序图（全部 `mermaid`）。
- ERD 与关键实体字段、唯一性约束、软删与审计策略。
- 一级资源、支撑资源、任务、发布制品的状态机定义。
- DoD：
- `R-ABS`、`R-DATA`、状态机相关设计输入全部有落点。
- 状态迁移触发条件、失败分支、补偿入口可追踪。
- 模型字段可直接映射到数据库表与索引策略。
- 进度：
- [x] 完成

### T3 契约与执行编排

- 目标：固化 API 契约与异步任务机制，形成统一执行面。
- 对应章节：`7`、`8`
- 输出物：
- OpenAPI 3.0 风格接口分组、路径规范、请求/响应 schema、错误模型。
- `Idempotency-Key`、`resource_version`、串行化键、重试/超时/取消/补偿机制。
- 关键流程时序图（`mermaid`）。
- DoD：
- 所有 CUD 接口满足异步任务提交，Query 保持同步语义。
- 错误码最小集合与业务错误场景一一对应。
- 能支持代码生成或直接指导 handler/service/repository 分层实现。
- 进度：
- [x] 完成

### T4 功能域与 K8s 编排

- 目标：把 10 个功能域需求落到可执行设计，明确模板、制品、部署与回滚细节。
- 对应章节：`4`、`9`
- 输出物：
- `4.1~4.10` 各资源域的创建/更新/删除/查询/发布规则与边界。
- Helm 模板路线、values 合并、镜像与凭证校验、命名标签规范。
- 部署/升级/回滚/漂移处理流程图（`mermaid`）。
- DoD：
- `R-ENV~R-OPS` 需求逐条可映射到章节条目。
- 共享资源与嵌入式资源在模型、API、权限、展示上隔离清晰。
- 可以直接指导编写各资源适配器与编排执行器。
- 进度：
- [x] 完成

### T5 安全、隔离、可运维与 NFR

- 目标：补全非功能与生产可用性约束，确保上线可治理、可审计、可扩展。
- 对应章节：`10`、`11`、`12`、`13`
- 输出物：
- 权限模型、租户隔离、Secret 生命周期、审计策略。
- 日志/指标/追踪/告警方案与运维手册。
- 性能预算、容量规划、压测与验收方案。
- DoD：
- 覆盖 `S-SEC` 与 `NFR-001~019` 的设计响应。
- 指标口径、采样维度、告警阈值具备可实施性。
- 关键高风险动作均可审计追溯。
- 进度：
- [x] 完成

### T6 验收与交付收口

- 目标：将设计文档收口成“可验收、可交付、可研发分解”的最终版本。
- 对应章节：`14`（不含 `14.4`）、`15`（不含 `15.2`）
- 输出物：
- 需求 ID -> 测试场景 -> 验收标准矩阵。
- 发布与回滚计划、交付清单、开放问题清单。
- 设计自检记录（冲突、遗漏、待决策项）。
- DoD：
- 形成端到端验收路径：需求 -> 设计 -> 用例 -> 发布。
- `docs/design.md` 完整性达到“指导编码”标准（满足 C-002）。
- 未决问题有明确 owner 与关闭条件。
- 进度：
- [x] 完成

### T7 契约一致性与覆盖补充

- 目标：补齐缺失接口定义与文档内路径示例，完成覆盖回标，消除“ID 已覆盖但接口条目不完整”的问题。
- 对应章节：`3.5`、`7.3`、`7.5`、`11.3`、`14.1`、`14.4`
- 输出物：
- 接口清单补齐结果（含模板、制品、发布记录、任务结果、发布包下载相关端点）。
- 文档内统一 canonical path（时序图、规则说明、验收用例中的接口示例一致）。
- `14.4` 覆盖状态回标与复核记录。
- DoD：
- `requirements.md`“资源接口分组”列出的端点在 `design.md` 有明确定义或有 ADR 说明的替代关系。
- 时序图、规则说明、接口章节引用同一套 canonical path。
- `14.4` 对受影响条目标注为真实状态（补齐后再恢复“已覆盖”）。
- 进度：
- [x] 完成

#### T7 子任务拆分

| Subtask ID | 子任务名称 | 影响章节（design.md） | DoD 摘要 | 状态 |
| --- | --- | --- | --- | --- |
| T7-01 | 资源接口清单补齐 | 7.3、7.5、14.4 | 补齐缺失端点（见 `T7-D1`） | DONE |
| T7-02 | canonical path 统一 | 3.5、11.3、14.1 | 统一接口示例路径（见 `T7-D2`） | DONE |
| T7-03 | 覆盖回标与回归验收 | 14.4、14.1 | 完成回标和证据补充（见 `T7-D3`） | DONE |

T7 DoD 细则：

- `T7-D1`：
- 端点补齐并落盘：
- `/templates`
- `/artifacts`
- `/devboxes/{devbox_id}/publishes`
- `/publishes/{publish_id}`
- `/tasks/{task_id}/result`
- `/applications/{application_id}/charts`
- `/charts/{chart_id}/package`
- OpenAPI 组织章节可追踪到上述端点。
- `T7-D2`：
- 时序图、隔离规则、验收用例中的接口示例使用同一 canonical path。
- 不再混用旧路径（如 `/applications/{id}/releases`）。
- `T7-D3`：
- 受影响条目先回标 `待补充`，完成后回标 `已覆盖`。
- 补充对应验收用例或证据项。

### T8 契约冲突修订（命名与幂等）

- 目标：修复契约层冲突（路径命名、更新方法、Service 自动命名、幂等字段落库），保证设计可实现且无双口径。
- 对应章节：`4.3`、`5.2.3`、`7.1~7.3`、`8.1~8.4`、`9.4`、`14.4`、`15.2`
- 输出物：
- API Path 命名约束修订方案（保持需求基线或 ADR 明确偏离）。
- Update Method 统一方案（`PUT`/`PATCH` 二选一并完成基线同步）。
- Service 自动命名统一规则（含适用场景与优先级）。
- 幂等字段与索引口径统一说明（字段、唯一约束、24h 去重、TTL）。
- DoD：
- 设计文档中不存在“路径命名规则冲突”与“更新方法冲突”双口径。
- 数据服务自动命名规则在 `4.3` 与 `9.4` 一致，且可直接用于实现。
- 幂等模型在实体定义、索引策略、任务编排三处一致。
- 如与需求基线存在偏离，已完成 ADR 与 `requirements.md` 同步修订，并在 `14.4` 标注真实状态。
- 进度：
- [x] 完成

#### T8 子任务拆分

| Subtask ID | 子任务名称 | 影响章节（design.md） | DoD 摘要 | 状态 |
| --- | --- | --- | --- | --- |
| T8-01 | API Path 命名冲突修正 | 7.3、14.4 | 修正连字符命名冲突（见 `T8-D1`） | DONE |
| T8-02 | Update 方法统一 | 7.3、14.4、15.2 | 统一更新方法口径（见 `T8-D2`） | DONE |
| T8-03 | Service 自动命名统一 | 4.3、9.4、14.4 | 统一 service 自动命名（见 `T8-D3`） | DONE |
| T8-04 | 幂等落库口径统一 | 5.2.3、5.4、8.1、8.4、14.4 | 统一 key/scope（见 `T8-D4`） | DONE |
| T8-05 | 冲突回标与变更留痕 | 14.4、15.2 | 回标并补 ADR 留痕（见 `T8-D5`） | DONE |

T8 DoD 细则：

- `T8-D1`：
- 修正 `data-services`、`lowcode-platforms`、`highcode-applications`、
  `agent-instances` 与“单词复数、避免连字符”约束冲突。
- 若保留现路径，必须 ADR 明确偏离并同步需求基线。
- `T8-D2`：
- 统一 Update 方法口径。
- 与需求基线一致，或通过 ADR + 需求修订给出替代契约。
- 在 `14.4` 反映状态。
- `T8-D3`：
- 统一 `svc-{application_name}-{component_alias}` 与
  `svc-{kind}-{resource_name}` 的适用边界。
- 同一场景只保留一条规则并给出冲突优先级。
- `T8-D4`：
- 消除 `idempotency_key` 与 `idempotency_scope` 在字段、索引、
  24h 去重窗口上的歧义。
- 明确 DDL 唯一约束与 TTL 清理策略。
- `T8-D5`：
- 受影响需求条目完成“待补充 -> 已覆盖”回标。
- 若有基线偏离，补齐 ADR 与需求修订记录。

### T9 语义覆盖补完与基线对齐

- 目标：修复“ID 已覆盖但需求语义仍有缺口/冲突”的问题，确保 `requirements.md` 与 `design.md` 在可实现口径上单一一致。
- 对应章节：`2.6`、`4.2`、`4.4`、`4.8`、`10.2`、`11.3`、`13.5`、`14.1`、`14.4`、`15.2`、`15.3`
- 输出物：
- 幂等唯一性口径统一结论（`idempotency_key` vs `idempotency_scope`）及基线同步记录。
- `R-ABS-007` 中“配额统计/审计报表口径”的设计落地条目（数据口径、读模型/统计路径、验收点）。
- `R-PKG-004`“外部环境导入成功”闭环设计与验收标准补齐（含 E2E 证据定义）。
- `embedded` 组件独立运维权限口径统一（超管与普通用户一致性规则）。
- `R-LCP-008` 默认值定稿（`allow_multi_instance` 默认策略、变更策略、回标状态）。
- DoD：
- 本轮识别的 5 个缺口均有独立子任务与完成证据，不允许“合并描述后无验收点”。
- 受影响条目在 `14.4` 的状态与备注与正文一致；若未补齐不得标记“已覆盖”。
- 若发生需求基线偏离，完成 `ADR + requirements/design/task` 三文档联动更新。
- 进度：
- [ ] 待开始

#### T9 子任务拆分

| Subtask ID | 子任务名称 | 关联需求 ID | 影响章节（design.md） | DoD 摘要 | 状态 |
| --- | --- | --- | --- | --- | --- |
| T9-01 | 幂等口径一致化 | R-OPS-002 | 见 `T9-D1` | 对齐口径并回标 | TODO |
| T9-02 | 配额与报表口径补齐 | R-ABS-007 | 见 `T9-D2` | 补齐落点与验收 | TODO |
| T9-03 | 外部导入闭环补齐 | R-PKG-004、R-PKG-006 | 见 `T9-D3` | 补齐 E2E 验收 | TODO |
| T9-04 | embedded 运维权限统一 | R-DSP-010、R-DSP-011 | 见 `T9-D4` | 消除权限冲突 | TODO |
| T9-05 | 多实例默认值定稿 | R-LCP-008 | 见 `T9-D5` | 定稿并收敛 OQ | TODO |

T9 DoD 细则：

- `T9-D1`：
- 在实体定义、索引、任务编排、接口契约处只保留一种幂等唯一性表达。
- 若最终口径与需求基线不同，必须先更新 `requirements.md` 并记录 ADR，再将 `14.4` 回标为“已覆盖”。
- `T9-D2`：
- 明确“5 类一级资源用于配额统计与审计报表”的数据口径（维度、聚合键、统计边界）。
- 至少补齐 1 个配额统计示例与 1 个审计报表示例到设计或验收章节。
- `T9-D3`：
- 在发布与验收章节明确外部导入流程（下载包 -> 外部环境导入 -> 运行验证）与通过条件。
- 用例证据至少包含：下载产物校验、外部导入命令/步骤结果、运行状态验证。
- `T9-D4`：
- 统一“嵌入式组件独立运维”权限策略，不允许权限矩阵与隔离策略双口径并存。
- 约束需覆盖 API 可见性、操作权限、审计动作三部分。
- `T9-D5`：
- 明确 `allow_multi_instance` 默认值、可配置范围、变更审批与生效时机。
- 关闭或更新 `15.3` 对应开放问题，保证正文与开放问题列表不冲突。

#### T7/T8 缺口到子任务映射（防遗漏）

| 缺口编号 | 缺口描述 | 覆盖子任务 |
| --- | --- | --- |
| GAP-01 | 接口分组存在缺失端点 | T7-01 |
| GAP-02 | 路径命名与 Update Method 不一致 | T8-01、T8-02 |
| GAP-03 | 文档内路径示例混用 | T7-02 |
| GAP-04 | 幂等唯一性口径冲突 | T8-04 |
| GAP-05 | 覆盖矩阵状态与正文不一致 | T7-03、T8-05 |
| GAP-06 | Service 自动命名规则前后冲突 | T8-03 |

#### T9 缺口到子任务映射（防遗漏）

| 缺口编号 | 缺口描述 | 覆盖子任务 |
| --- | --- | --- |
| GAP-07 | 幂等唯一性定义在需求与设计间存在语义偏移 | T9-01 |
| GAP-08 | `R-ABS-007` 的配额统计/审计报表口径缺设计落点 | T9-02 |
| GAP-09 | `R-PKG-004` 缺少“外部环境导入成功”闭环验收定义 | T9-03 |
| GAP-10 | 嵌入式组件独立运维权限存在矩阵与正文冲突 | T9-04 |
| GAP-11 | `R-LCP-008` 默认值与开放问题描述不一致 | T9-05 |

#### T8 本轮复核冲突映射（2026-02-23）

| 复核项 ID | 冲突描述 | 证据（requirements/design） | 对应子任务 |
| --- | --- | --- | --- |
| RC-01 | API Path 命名冲突 | `req:389` vs `des:1191` | T8-01 |
| RC-02 | Update Method 冲突 | `req:395` vs `des:1205` | T8-02 |
| RC-03 | Service 自动命名规则冲突 | `design.md:515` vs `design.md:1576` | T8-03 |
| RC-04 | 幂等口径冲突（key vs scope） | `design.md:807` vs `design.md:1339` | T8-04 |

## 5. 统一完成判定（Definition of Done）

- [ ] `docs/design.md` 1~15 章全部完成，且与 `docs/requirements.md` 语义一致（含 T9 补齐项）。
- [x] 所有图均为 `mermaid`，渲染可读、语义完整。
- [x] API 设计符合 OpenAPI 3.0 + RESTful，并落地统一响应与错误码规范。
- [ ] 需求覆盖审计完成且通过语义复核，关键需求簇无“已覆盖但语义缺口”项。
- [x] 文档达到可指导编码级别：研发可据此拆分模块并开始实现。
- [x] T7 补充任务（契约一致性与覆盖回标）完成并关闭。
- [x] T8 补充任务（契约冲突修订与基线留痕）完成并关闭。
- [ ] T9 补充任务（语义覆盖补完与基线对齐）完成并关闭。
